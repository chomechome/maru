language: python

python:
  - 3.6

install:
  - "pip install poetry==0.12.10"
  - "poetry install --no-interaction"
  - "poetry shell"

jobs:
  include:
    - stage: codestyle
      python: 3.6
      script:
        - "make lint"
    - stage: test
      python: 3.6
      script:
        - "make test"
    - stage: coverage
      python: 3.6
      script:
        - "make coverage"
